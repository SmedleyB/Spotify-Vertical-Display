# Spotify API Credentials
# Get these from: https://developer.spotify.com/dashboard/
SPOTIPY_CLIENT_ID=your_client_id_here
SPOTIPY_CLIENT_SECRET=your_client_secret_here

# Redirect URI (must match the one configured in your Spotify App settings)
SPOTIPY_REDIRECT_URI=http://127.0.0.1:5000/callback

# Optional: Custom cache path for Spotify tokens (defaults to .spotify-token-cache)
# SPOTIPY_CACHE_PATH=.spotify-token-cache

# Optional: Enable debug logging (set to 'true' for detailed logs)
# DEBUG=false
